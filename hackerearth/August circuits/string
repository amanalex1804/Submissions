#include<bits/stdc++.h>
using namespace std;
unordered_map<char,int>M;
int main(){
	int n;cin>>n;
	string s;cin>>s;
	int mx=0;
	for(int i=0;i<n;++i)
	M[s[i]]+=1,mx=max(mx,M[s[i]]);
	cout<<(n-mx);
	
	return 0;
}
