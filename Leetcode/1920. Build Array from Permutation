class Solution {
    public int[] buildArray(int[] nums) {

        int sz = (int)nums.length;
        
        int [] num1 = new int[sz];

        for(int i=0;i<sz;++i){
            int val = nums[i];
            num1[i] =nums[val];
        }
        return num1;
        
    }
}
