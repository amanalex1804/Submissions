#include <iostream>
using namespace std;

int main() {
	int t;cin>>t;
	while(t--){
	    int n;cin>>n;
	    int ans=0,temp = n;
	    while(temp>0){
	        int x = temp%10;
	        ans+=(x*x*x);
	        temp/=10;
	    }
	    if(ans==n) cout<<"Yes\n";
	    else cout<<"No\n";
	}
	return 0;
}
