#include <bits/stdc++.h>
using namespace std;
#define ll long long int 
int main() {
	int t;cin>>t;
	while(t--){
	    ll n;cin>>n;
	    ll ans = sqrt(n);
	    if(ans*ans==n) --ans;
	    cout<<ans<<"\n";
	}
	return 0;
}
