#include <bits/stdc++.h>
using namespace std;
typedef long long int ll;
int main() {
	int t;cin>>t;
	while(t--){
	    ll a,b;cin>>a>>b;
	    ll c= __gcd(a,b);
	    cout<<(a*b)/c<<" "<<c<<"\n";
	}
	return 0;
}
