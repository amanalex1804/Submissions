#include <bits/stdc++.h>
using namespace std;

int main() {
	int t;cin>>t;
	while(t--){
	    int m,n;cin>>n>>m;
	    long long int ans =0;
	    while(m>0 and n>0){
	        ans+=(m*n);
	        --m;--n;
	    }
	    cout<<ans<<"\n";
	}
	return 0;
}
