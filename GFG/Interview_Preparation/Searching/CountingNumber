#include<bits/stdc++.h>
using namespace std;
int main()
 {
	int t;
	cin>>t;

	while(t--){
	  int n,k;cin>>n>>k;
	  int cnt =0;
	  for(int i=0,x;i<n;++i){
	      cin>>x;
	      if(k==x) ++cnt;
	  }
	  if(!cnt) cnt =-1;
	  cout<<cnt<<"\n";
	}
	return 0;
}
