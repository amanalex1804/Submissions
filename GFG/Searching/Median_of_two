#include <bits/stdc++.h>
using namespace std;

int main() {
	int t;cin>>t;
	while(t--){
	    int n;cin>>n;
	    int A[n+5];
	    for(int i=1;i<=n;++i) cin>>A[i];
	    sort(A+1,A+n+1);
	    if(n%2==0) cout<<(A[n/2]+A[(n/2)+1])/2<<"\n";
	    else cout<<A[(n/2)+1]<<"\n";
	}
	
	return 0;
}
