#include <stdio.h>
#include<iostream>
using namespace std;

int main() {
	int t; cin>>t;
	string s, ans;
	while(t--){
	 cin>>s;
	 int l = (int)s.size();
     ans = s[0];
     for(int i=1;i<l-1;i+=2) ans+=s[i];
     ans+= s[l-1];
     cout<<ans<<"\n";
 	

	}
  
	
	return 0;
}
