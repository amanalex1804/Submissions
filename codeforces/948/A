#include<bits/stdc++.h>
using namespace std;
typedef long long int ll;
int main(){
	int n;
	cin>>n;
	ll A[n+5];
	for(int i=1;i<=n;++i) cin>>A[i];
	sort(A+1,A+n+1);
	if(n&1){
		ll mid=n/2;
		cout<<A[mid+1];
	}
	else{
		ll mid=n/2;
		cout<<A[mid];
	}
	
	return 0;
}
