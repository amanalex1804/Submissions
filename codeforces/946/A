#include<bits/stdc++.h>
using namespace std;

int main()
{
		ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	long long int sp,sn,n,i,x;
	sp=0;sn=0;
	cin>>n;
	for(i=0;i<n;++i)
	{
		cin>>x;
		if(x<0) sn+=x;
		else sp+=x;
	}
	cout<<sp-sn;
	return 0;
}
