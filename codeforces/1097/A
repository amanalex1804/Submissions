#include<bits/stdc++.h>
using namespace std;

int main(){
	
	string s;cin>>s;
	bool flag=false;
	for(int i=0;i<5;++i){
		string f;cin>>f;
		if(s[0]==f[0] or s[1]==f[1]){
			flag=true;
		}
	}
	if(flag) cout<<"YES";
	else cout<<"NO";
	return 0;
}
