#include<bits/stdc++.h>
using namespace std;
#define ll long int
int main(){
	// ios_base::sync_with_stdio(false);cin.tie(NULL);
	
	ll n; cin>>n;
	
	map<ll,bool>M;
	
	while(1){
		if(M[n]==true) break;
		M[n] = true;
		n=(n+1);
		while(n%10==0) n/=10;
	}
	
	cout<<(int)M.size();
	
	
	return 0;
}
