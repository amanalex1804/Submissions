#include<bits/stdc++.h>
using namespace std;

int main(){
	int n; cin>>n;
	map<int,int>M;
    int mx=-1,pos;
    for(int i=0,x;i<n;++i){
    	cin>>x;
    	M[x]++;
    	if(M[x]>mx) { mx=M[x];		}
	}
	cout<<mx;
	
	return 0;
}
